package general_tasks;

public class ternaryOperators {

    public static void main(String[] args) {

        int age = 25;

        String result = (age > 25) ? "Your age is older than 25" : "Your age is not older than 25";
        System.out.println(result);
    }
}
